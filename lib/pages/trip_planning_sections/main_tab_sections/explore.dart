import 'package:flutter/material.dart';

class ExploreTrips extends StatelessWidget {
  const ExploreTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Explore')));
  }
}
