import 'package:flutter/material.dart';

class OverviewTrips extends StatelessWidget {
  const OverviewTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Information')));
  }
}
