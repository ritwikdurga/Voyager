import 'package:flutter/material.dart';

class ExpensesTrips extends StatelessWidget {
  const ExpensesTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Expenses')));
  }
}
